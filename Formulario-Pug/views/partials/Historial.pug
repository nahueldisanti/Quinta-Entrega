h1 Productos
if productos == ""
    br
    h3 No hay ningún producto agregado.
else
    table(class="table")
        thead
            tr
                th(scope="col") id
                th(scope="col") Nombre
                th(scope="col") Precio
                th(scope="col") Imagen 
        tbody
            each item in items
                tr
                    th(scope="row")= item.id
                    td= item.title
                    td= item.price 
                    td
                        img(src=prod.url, alt=item.title, width="25")

